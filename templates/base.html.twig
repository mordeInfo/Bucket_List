<!DOCTYPE html>
<html lang="fr" xmlns="http://www.w3.org/1999/html">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Bucket-List{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">

    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}

</head>
<body>
<header>


    <h1 class="header-logo"><img src="{{ asset('media/logo.svg') }}" alt="">Bucket-list</h1>
    <a href="{{ path('main_home') }}">Home</a>
    <a href="{{ path('main_about_us') }}">About us</a>
    <a href="{{ path('wish_list') }}">Wish List</a>
    <a href="{{ path('wish_ajout') }}">Add Wish</a>


</header>
<main>
    <div class="marge_main">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="{{ label }}">{{ message }}</div>
            {% endfor %}

        {% endfor %}
        {% block body %}{% endblock %}
    </div>
</main>
<footer>
    <div class="container">

        <p> © Bucket-list {{ 'now' | date("Y") }}</p>
    </div>
</footer>

</body>
</html>
